# Scripts de migração/SQL usados uma vez no SQL Editor do Supabase --
# não fazem parte do app publicado, não sobem pro GitHub.
*.sql

# Configuração local do Claude Code (pessoal, não compartilhada)
.claude/settings.local.json
